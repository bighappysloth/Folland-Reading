\usepackage{import}
\import{./}{packages.tex}
\import{./}{headers.tex}
% Commands

    % Essentials
    \renewcommand{\Re}{\operatorname{Re}} % Real Part
    \renewcommand{\Im}{\operatorname{Im}} % Imaginary Part
    \newcommand{\defined}{\coloneqq} % := sign
    
    % Fonts
    \newcommand{\bb}[1]{\mathbb{#1}} %shortcut for mathbb
    \renewcommand{\cal}[1]{\mathcal{#1}} %shortcut for mathcal
    
    % Common Sets
    \newcommand{\real}{\mathbb{R}}
    \newcommand{\nat}{\mathbb{N}}
    \newcommand{\integer}{\mathbb{Z}}
    
    % Operators
    \newcommand{\range}{\operatorname{range}} %Range of a function
    \newcommand{\dom}{\operatorname{dom}} %Domain of a Function
    \newcommand{\codom}{\operatorname{codom}} % Codomain
    \newcommand{\id}[1]{\operatorname{id}_{#1}} % Identity Mapping
    \newcommand{\sgn}{\operatorname{sgn}} %Signum function
    
    \newcommand{\wt}{\operatorname{wt}} %Hamming weight
    \newcommand{\image}{\operatorname{im}} %Image of a linear map or any map in general
    
    % Norms
    \newcommand{\norm}[1]{\lVert {#1} \rVert} % || . ||
    \newcommand{\bignorm}[1]{\left\lVert {#1} \right\rVert} % || . || but larger
    
    % Topology
    \newcommand{\acc}{\operatorname{acc}} % Set of Accumulation Points
    \newcommand{\abrackets}[1]{\langle {#1} \rangle} % Angled Brackets for Nets
    \newcommand{\nb}[1]{\mathcal{N}_B({#1})} %N_b(x) is a neighbourhood of x
    \newcommand{\Tau}{\mathcal{T}} % Big Tau Denoting Topological Space
    \newcommand{\cl}[1]{\overline{#1}} % Closure of a set
    \newcommand{\clc}[1]{(\overline{#1})^c} %  Complement of the Closure
    \newcommand{\Epsilon}{\mathcal{E}} % Base, generating set for Topology and Sigma algebras
    
    % Projection Maps
    \newcommand{\pmap}[2]{\pi_{#1}({#2})} % projection maps 
    \newcommand{\pnv}[2]{\pi_{#1}^{-1}({#2})} %inverse projection maps
    
    % Functions on a Topological Space
    \newcommand{\bc}[1]{\operatorname{BC}({#1})} %Bounded and Continuous Functions
    \newcommand{\cc}[1]{\operatorname{C}_c({#1})} %Continuous on a Compact Set
    \newcommand{\cnot}[1]{\operatorname{C}_0({#1})} %Vanishes at Infinity
    \newcommand{\supp}[1]{\operatorname{supp}{(#1)}} %Support of a function

% Settings
\setlength{\parindent}{0pt} %default no indent


%always put this at last
\usepackage{subfiles}
